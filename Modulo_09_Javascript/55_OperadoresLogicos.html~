
<!doctype html>
<html lang="pt-br">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    
    <title>Titulo</title>

	<script type="text/javascript">

//--------------------------------- IF ELSE: INTRODUCAO --(video 137)--------
		var condicao  = true;

		if (condicao) {
			document.write('condicao é true');
		} else {
			document.write('condicao é false');
		}


		if (condicao2) {

		} if else (condicao3) {

		} if else (condicao4) {

		} else { //esse último else é opcional
		
		}

//-----------------------------------------------------------

/*---------------OPERADORES DE COMPARAÇÃO ----------(video 138)-------*/

		if (a==b) { } //igual

		if (a===b) { } //idêntico

		if (a!=b) { } //diferente

		if (a<>b) { } //diferente

		if (a!==b) { } //não idêntico
		
		(...)

//---------------IF ELSE: PARTE 1 -----------------(video 139)---------

		if (2 == 2) {} //true
	
		if (2 === '2') {} //false
	
		if (2 != 1) {} //true
	
		if (2 !== 2) {} //são diferentes ou de tipo diferente? R: false

		if (2 !== 5) {} //são diferentes ou de tipo diferente? R: true

		if (2 !== '2') {} //são diferentes ou de tipo diferente? R: true

	
		if ('y' == 'x') {} //false

		var nota = prompt('Digite a nota do aluno:');
		var media=6;

		/*Abaixo, a pessoa digita um número e na comparação esse char/string é convertido em número
		  sendo fiel na comparação com a variável media (que é um número */
		if (nota >= media) { document.write('Aluno aprovado'); } 
			else { document.write('Aluno reprovado'); } 
	
	
	</script>



  </head>
  <body>


  </body>
</html>
